"""Vendored binaries shipped with methoddep for offline deployments.

Sub-packages here embed small native tools so they don't need to be
installed separately on target machines (where NuGet / apt / choco
access may be unavailable).
"""
